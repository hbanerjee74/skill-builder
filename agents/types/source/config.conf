# Type configuration for source agents
NAME_PREFIX=source

# Focus lines per phase (one sentence each)
FOCUS_LINE__research_concepts=Focus on data extraction patterns, API structures, authentication flows, rate limits, and source-specific data quality considerations.
FOCUS_LINE__research_patterns_and_merge=Emphasize extraction strategies, pagination patterns, webhook handling, and source-specific data freshness guarantees.
FOCUS_LINE__reasoning=Pay special attention to API rate limit implications, data completeness trade-offs, and incremental vs. full extraction decisions.
FOCUS_LINE__build=Target extraction pipeline patterns. Content should help engineers understand source WHAT and WHY — API endpoint behaviors, data schemas, quality gotchas.
FOCUS_LINE__validate_and_test=Focus on data extraction patterns, API structures, authentication flows, rate limits, and source-specific data quality considerations.

# Entity examples (used in research-concepts template)
ENTITY_EXAMPLES=e.g., for Stripe: charges, subscriptions, events; for Salesforce: accounts, opportunities, custom objects
