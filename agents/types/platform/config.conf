# Type configuration for platform agents
NAME_PREFIX=platform

# Focus lines per phase (one sentence each)
FOCUS_LINE__research_concepts=Focus on tool capabilities, API patterns, integration constraints, and platform-specific configuration.
FOCUS_LINE__research_patterns_and_merge=Focus on tool capabilities, API patterns, integration constraints, and platform-specific configuration.
FOCUS_LINE__reasoning=Focus on tool capabilities, API patterns, integration constraints, and platform-specific configuration.
FOCUS_LINE__build=Focus on tool capabilities, API patterns, integration constraints, and platform-specific configuration.
FOCUS_LINE__validate=Focus on tool capabilities, API patterns, integration constraints, and platform-specific configuration.
FOCUS_LINE__test=Focus on tool capabilities, API patterns, integration constraints, and platform-specific configuration.

# Entity examples (used in research-concepts template)
ENTITY_EXAMPLES=e.g., for Terraform: providers, modules, resources; for Kubernetes: deployments, services, ingress
