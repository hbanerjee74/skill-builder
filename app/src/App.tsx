// App shell is now handled by the router and app-layout.
// This file is kept for compatibility but is no longer used as the entry point.
// See src/router.tsx and src/components/layout/app-layout.tsx.
export default function App() {
  return null;
}
