{
  "name": "skill-builder-sidecar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm update @anthropic-ai/claude-agent-sdk --silent && node build.js",
    "dev": "node --loader ts-node/esm agent-runner.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.37"
  },
  "devDependencies": {
    "@types/node": "^22",
    "esbuild": "^0.25",
    "typescript": "^5.8",
    "vitest": "^4.0.18"
  }
}
