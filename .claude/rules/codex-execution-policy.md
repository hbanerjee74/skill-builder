# Codex Execution Policy

- Execute directly by default using available tools.
- Use sub-agents only for parallelizable research or heavyweight review.
- Prefer deterministic direct execution for git/gh/linear operations.
- Avoid orchestration overhead when direct execution is faster and safer.
